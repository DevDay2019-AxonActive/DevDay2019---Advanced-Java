1. Apply Basic Java Future
	a. apply Future with Callable by using executor, submit(), get() in DefaultSearchingService
	b. get(time, timeunit) to generate a TimeoutException
	c. isDone(), isCancel(), cancel() to verify whether futre is finished or not